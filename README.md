# XO Game

เกม XO ข้ามแพลตฟอร์มที่พัฒนาด้วย Flutter โปรเจคนี้รองรับแพลตฟอร์ม Android, iOS, Web, macOS, Linux และ Windows

## การติดตั้ง

1. **โคลน repository:**
   ```bash
   git clone https://github.com/nwrm/xo.git
   cd xo
2. ติดตั้ง Flutter
3. ตรวจสอบการตั้งค่า Flutter:
   ตรวจสอบสภาพแวดล้อมการพัฒนาด้วยคำสั่ง:
      flutter doctor
4. ติดตั้ง dependencies:
   ไปที่ไดเรคทอรีของโปรเจคและรันคำสั่ง:
      flutter pub get

การรันโปรแกรม
เพื่อรันโปรแกรมบนแพลตฟอร์มต่างๆ ใช้คำสั่งดังนี้:
Android:
  flutter run -d android

การออกแบบโปรแกรม
เกม XO ออกแบบด้วย UI ที่ง่ายต่อการใช้งาน โดยแยกตรรกะของเกมออกจากเลเยอร์การแสดงผลอย่างชัดเจน องค์ประกอบหลักประกอบด้วย:

1.กระดานเกม:
  ตารางขนาด 3x3 (สามารถปรับเปลี่ยนได้) ที่แทนกระดานเกม XO

2.สถานะของเกม:
  จัดการสถานะของเกม รวมถึงผู้เล่นปัจจุบัน สถานะกระดาน และผลลัพธ์ของเกม

3.ส่วนติดต่อผู้ใช้:
  สร้างด้วย Flutter widgets เพื่อให้การออกแบบมีการตอบสนองและสวยงาม

algorithm
algorithmการตรวจสอบผู้ชนะ
อalgorithmสำหรับตรวจสอบผู้ชนะในเกม XO ถูกนำมาใช้งานดังนี้:

ตรวจสอบแถว:
วนลูปตรวจสอบทุกแถว (y).
ใช้ every เพื่อตรวจสอบว่าแถวทั้งหมดเป็นของผู้เล่น (player) หรือไม่.

ตรวจสอบคอลัมน์:
วนลูปตรวจสอบทุกคอลัมน์ (x).
ใช้ every เพื่อตรวจสอบว่าแต่ละเซลล์ในคอลัมน์นั้นเป็นของผู้เล่น (player) หรือไม่.

ตรวจสอบเส้นทแยงมุม:
สร้างลิสต์ที่ประกอบไปด้วยค่าในเส้นทแยงมุมหลักโดยใช้ List.generate.
ใช้ every เพื่อตรวจสอบว่าเซลล์ทั้งหมดในเส้นทแยงมุมหลักเป็นของผู้เล่น (player) หรือไม่.

ตรวจสอบเส้นทแยงมุมรอง:
สร้างลิสต์ที่ประกอบไปด้วยค่าในเส้นทแยงมุมรองโดยใช้ List.generate.
ใช้ every เพื่อตรวจสอบว่าเซลล์ทั้งหมดในเส้นทแยงมุมรองเป็นของผู้เล่น (player) หรือไม่.

คืนค่า true ถ้ามีการตรวจพบว่าผู้เล่นคนใดคนหนึ่งชนะในแถว คอลัมน์ เส้นทแยงมุมหลัก หรือเส้นทแยงมุมรอง
ด้วยalgorithmนี้ เราสามารถตรวจสอบได้อย่างถูกต้องว่าผู้เล่นคนใดชนะในสถานการณ์ปัจจุบันของกระดานเกม

bool _checkWin(String player) {
// ตรวจสอบแถว
for (int y = 0; y < gridSize; y++) {
if (board[y].every((cell) => cell == player)) {
return true;
}
}

// ตรวจสอบคอลัมน์
for (int x = 0; x < gridSize; x++) {
if (board.every((row) => row[x] == player)) {
return true;
}
}

// ตรวจสอบเส้นทแยงมุมหลัก
if (List.generate(gridSize, (index) => board[index][index]).every((cell) => cell == player)) {
return true;
}

// ตรวจสอบเส้นทแยงมุมรอง
if (List.generate(gridSize, (index) => board[index][gridSize - 1 - index]).every((cell) => cell == player)) {
return true;
}

return false;
}


